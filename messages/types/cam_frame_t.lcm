package raccoon;

struct cam_frame_t
{
    int64_t     timestamp;
    int32_t     frame_width;
    int32_t     frame_height;
    int32_t     frame_size;
    byte        frame_data[frame_size];     // JPEG-encoded image
    int32_t     num_detections;
    cam_blob_t  detections[num_detections];
}
