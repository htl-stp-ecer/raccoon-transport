name: raccoon_transport
description: Shared transport library with channel definitions and optional reliable delivery
version: 0.1.0
repository: https://github.com/user/raccoon-transport

environment:
  sdk: ^3.0.0

dependencies:
  meta: ^1.16.0

dev_dependencies:
  test: ^1.24.0
