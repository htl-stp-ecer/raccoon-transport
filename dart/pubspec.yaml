name: raccoon_transport
description: Shared LCM transport library with channel definitions, code generation, and optional reliable delivery
version: 0.1.0
repository: https://github.com/user/raccoon-transport

environment:
  sdk: ^3.0.0

dependencies:
  build: ^3.1.0
  build_config: ^1.1.0
  glob: ^2.1.0

dev_dependencies:
  build_runner: ^2.4.0
  build_test: ^3.1.0
  test: ^1.24.0
